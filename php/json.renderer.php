<?php

function render_header($options=array('title'=>TITLE_DEFAULT)) {
	$header = '';
	return $header;
}

function render_footer($options=array('title'=>TITLE_DEFAULT)) {
	$footer = '';
	return $footer;
}
